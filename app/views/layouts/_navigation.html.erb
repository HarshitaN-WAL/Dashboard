<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%if session[:user_id]%>
    <%= link_to "KT", welcome_about_path,class:"navbar-brand", id:"logo"%>
  <%else%>
    <%= link_to "KT", root_path,class:"navbar-brand", id:"logo"%>
  <%end%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div>
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <!-- <a class="nav-link" href="#">Log in</a> -->
        <% if session[:user_id]%>
          <%= link_to "Projects", projects_path, class:"nav-link"%>
        <%end%>
      </li>
      <li class="nav-item">
        <!-- <a class="nav-link" href="#">Log in</a> -->
        <% if session[:user_id] && ( @role == "Top Management" || @role == "Admin")%>
        <%= link_to "Users", users_path, class:"nav-link"%>
        <%end%>
      </li>
      <li class="nav-item">
        <%if session[:user_id] && @role == "Admin"%>
            <%= link_to "Roles", roles_path, class:"nav-link"%>
        <%end%>
      </li>
    </ul>
  </div>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto left-link">
      <!-- <li class="nav-item"> -->
        <!-- <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a> -->
        <%#= link_to "About", welcome_about_path, class:"nav-link"%>
      <!-- </li> -->
      <% if session[:user_id]%>
        <li class="nav-item">
          <%= link_to "Profile", user_path(current_user), class:"nav-link"%>
        </li>
      <%end%>
      <li class="nav-item">
        <!-- <a class="nav-link" href="#">Log in</a> -->
        <% if session[:user_id]%>
        <%= link_to "Logout", logout_path, class:"nav-link"%>
        <% else %>
        <%= link_to "Login", login_path, class:"nav-link"%>
        <%end%>
      </li>
    </ul>
  </div>
</nav>